apiVersion: v1
kind: Service
metadata:
  annotations: {service.beta.kubernetes.io/azure-load-balancer-internal: 'true'}
  labels: {fint.stack: administrasjon-fullmakt, fint.role: consumer}
  name: consumer-administrasjon-fullmakt
spec:
  type: LoadBalancer
  ports:
    - {name: '8080', port: 8080}
  selector: {fint.stack: administrasjon-fullmakt, fint.role: consumer}
---
apiVersion: v1
kind: Service
metadata:
  annotations: {service.beta.kubernetes.io/azure-load-balancer-internal: 'true'}
  labels: {fint.stack: administrasjon-fullmakt, fint.role: provider}
  name: provider-administrasjon-fullmakt
spec:
  type: LoadBalancer
  ports:
    - {name: '8080', port: 8080}
  selector: {fint.stack: administrasjon-fullmakt, fint.role: provider}
